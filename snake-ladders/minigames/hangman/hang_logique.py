"""
    Fichier contenant la machine à état contrôlant le jeu hangman logiquement avec les echecs/outils et la banque de mots
    Realiser par Cassey Martin et Jake Chagnon
"""

# random.randint(0, W - 20) import random


class hang_logique:

    @staticmethod
    def millisToSeconds(millis):
        return float(millis / 1000)