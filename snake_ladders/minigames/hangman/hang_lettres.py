"""
    Fichier contenant les lettres avec leur translation approprier pour le mini jeu de hangman
    Realiser par Cassey Martin et Jake Chagnon
"""
import pygame

#  font = pygame.font.SysFont(None, 36)
#paddle = pygame.Rect(W // 2 - 60, H - 20, 120, 10)
#block = pygame.Rect(random.randint(0, W - 20), 0, 20, 20)
# Display score
#score_text = font.render(f"Score: {score}", True, WHT)
# pygame.font.Font()
# font.render()

